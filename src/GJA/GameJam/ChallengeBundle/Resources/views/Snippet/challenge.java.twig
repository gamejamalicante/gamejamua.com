HttpClient challengeClient = HttpClients.createDefault();
HttpPost challenge = new HttpPost("{{ url('gamejam_api_challenge_complete', {challenge: challenge.token}) }}");

try {
    challengeClient.execute(challenge);
}
catch (Exception e)
{
}