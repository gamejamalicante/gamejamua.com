#include <curl/curl.h>

CURL* curl;
char* url = "{{ url('gamejam_api_challenge_complete', {challenge: challenge.token}) }}";

curl = curl_easy_init();
curl_easy_setopt(curl, CURLOPT_URL, url);
curl_easy_perform(curl);
