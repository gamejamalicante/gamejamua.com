{% extends 'GameJamCompoBundle::layout.html.twig' %}

{% block content %}
    <div class="section profile">
        <div id="loginbox" style="float: right;" class="mainbox col-md-6 col-sm-8">
            <div class="panel panel-primary" >
                <div class="panel-heading">
                    <div class="panel-title">Introduce tu nombre de usuario / email</div>
                </div>

                <div style="padding-top:30px" class="panel-body">
                    {% if invalid_username is defined %}
                        <div class="alert alert-danger">Los datos introducidos son incorrectos</div>
                    {% endif %}
                    <div style="display:none" id="login-alert" class="alert alert-danger col-sm-12"></div>
                    <form id="loginform" class="form-horizontal" role="form" action="{{ path('fos_user_resetting_send_email') }}" method="post">
                        <div style="margin-bottom: 25px" class="input-group">
                            <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
                            <input type="text" class="form-control" id="username" name="username" placeholder="Nombre de usuario / email" required="required" />
                        </div>
                        <div style="margin-top:10px" class="form-group">
                            <!-- Button -->
                            <div class="col-sm-12 controls">
                                <button type="submit" id="btn-login" class="btn btn-green"><i class="fa fa-envelope"></i> Enviar instrucciones</button>
                            </div>
                        </div>
                    </form>

                </div>
            </div>
        </div>
        <h1>Recuperar contraseña</h1>

        <p>Introduce tus nombre de usuario / email en el formulario y te enviaremos instrucciones para resetear tu contraseña.</p>
        <div class="clearfix"></div>
    </div>
{% endblock %}