{% extends 'GameJamCompoBundle::layout.html.twig' %}

{% block content %}
    <div class="section profile">
        <div id="loginbox" style="float: right;" class="mainbox col-md-6 col-sm-8">
            <div class="panel panel-primary" >
                <div class="panel-heading">
                    <div class="panel-title">Introduce tu nueva contraseña</div>
                </div>

                <div style="padding-top:30px" class="panel-body">
                    {% if not form.vars.valid %}
                        <div class="alert alert-danger">
                            {{ form_errors(form) }}
                            {{ form_errors(form.plainPassword.first) }}
                        </div>
                    {% endif %}

                    <div style="display:none" id="login-alert" class="alert alert-danger col-sm-12"></div>
                    <form id="loginform" class="form-horizontal" role="form" action="{{ path('fos_user_resetting_reset', {'token': token}) }}" {{ form_enctype(form) }} method="post" autocomplete="off">
                        <div style="margin-bottom: 25px" class="input-group">
                            <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
                            {{ form_widget(form.plainPassword.first, {'attr':{'class':'form-control', 'placeholder': 'Nueva contraseña'}}) }}
                        </div>

                        <div style="margin-bottom: 25px" class="input-group">
                            <span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
                            {{ form_widget(form.plainPassword.second, {'attr':{'class':'form-control', 'placeholder': 'Repetir contraseña'}}) }}
                        </div>
                        <div style="margin-top:10px" class="form-group">
                            <div class="col-sm-12 controls">
                                <button type="submit" id="btn-login" class="btn btn-green">Cambiar contraseña</button>
                            </div>
                        </div>
                        {{ form_rest(form) }}
                    </form>

                </div>
            </div>
        </div>
        <h1>Recuperar contraseña</h1>

        <p>Introduce tu nueva contraseña en el cuadro de la derecha y podrás volver a entrar en el sistema.</p>
        <div class="clearfix"></div>
    </div>
{% endblock %}
