{% extends 'GameJamCompoBundle::layout.html.twig' %}

{% block content %}
<div class="section profile">
    <div id="loginbox" class="mainbox col-md-6 col-sm-8 pull-right">
        <div class="panel panel-primary" >
            <div class="panel-heading">
                <div class="panel-title">Introduce tus datos de accesso</div>
            </div>

            <div style="padding-top:30px" class="panel-body">
                {% if error %}
                    <div class="alert alert-danger">Los datos introducidos son incorrectos</div>
                {% endif %}

                <div style="display:none" id="login-alert" class="alert alert-danger col-sm-12"></div>
                <form id="loginform" class="form-horizontal" role="form" action="{{ path("fos_user_security_check") }}" method="post">
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                    <div style="margin-bottom: 25px" class="input-group">
                        <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
                        <input type="text" class="form-control" id="username" name="_username" placeholder="Nombre de usuario / email" value="{{ last_username }}" required="required" />
                    </div>

                    <div style="margin-bottom: 25px" class="input-group">
                        <span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
                        <input type="password" class="form-control" id="password" name="_password" placeholder="Contraseña" required="required" />
                    </div>
                    <div class="input-group">
                        <div class="checkbox">
                            <label>
                                <input type="checkbox" id="remember_me" name="_remember_me" value="on" /> Recordar sesión
                            </label>
                        </div>
                    </div>
                    <div style="margin-top:10px" class="form-group">
                        <!-- Button -->
                        <div class="col-sm-12 controls">
                            <button type="submit" id="btn-login" class="btn btn-green">¡Entrar!</button>
                        </div>
                    </div>
                </form>

            </div>
        </div>
    </div>
    <h1>Entrar en el sistema</h1>

    <p>Introduce tus datos de acceso para acceder al sistema y poder inscribirte en nuestras GameJams. Si no estás registrado aún puedes hacerlo pulsando este enlace.</p>
    <p><a href="{{ path('fos_user_registration_register') }}" class="btn btn-facebook"><i class="fa fa-pencil"></i> ¡Registrarse!</a></p>
    <p><a href="{{ path('fos_user_resetting_request') }}" class="btn"><i class="fa fa-key"></i> He olvidado mi contraseña</a></p>
    <div class="clearfix"></div>
</div>
{% endblock %}
