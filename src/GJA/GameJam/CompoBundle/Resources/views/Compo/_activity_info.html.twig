<div class="item achievement">
    <img class="avatar" src="{{ user_avatar(item.user) }}" />
    <h3><a href="{{ path('gamejam_user_profile', {user: item.user.username}) }}">{{ item.user }}</a> ha actualizado la información del juego <strong><a href="{{ path('gamejam_game', {game: item.game.nameSlug}) }}">{{ item.game }}</a></strong></h3>
    <span class="date"><i class="fa fa-clock-o"></i> {{ time_diff(item.date) }}</span>
    <div class="clearfix"></div>
</div>
